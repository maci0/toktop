# Dependencies of scripts/screenshot.py (renders a tmux ANSI capture to PNG).
# Install into a scratch venv, not the system: uv pip install -r scripts/requirements.txt
# Exact pins: a newer release must arrive as a reviewed bump, not an implicit install.
# pyte is LGPLv3 (wcwidth and pillow are MIT). This renderer is a local developer
# tool and is not linked into the toktop binary.
# Wheel sha256 on the pure-Python packages: a registry swap of those files
# fails the install. Pillow ships per-platform wheels, so it stays a version
# pin only; hashing one wheel would refuse every other OS/arch/CPython.
pyte==0.8.2 \
    --hash=sha256:85db42a35798a5aafa96ac4d8da78b090b2c933248819157fc0e6f78876a0135
wcwidth==0.8.4 \
    --hash=sha256:2097bb1d28a0ba8fe177c2eb607317f9b1627b03f33ebebfd3da670b337a65ba \
    --hash=sha256:2dae09efa25253ae2874188e86d6861af3b1652aef4118cdf3f0bda288a957fb
pillow==12.3.0
